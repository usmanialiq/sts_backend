"use strict";
// Auth Routes
const login = "/login";
const register = "/register";

// Users
const users = "/users";
const students = "/students";

module.exports = {
    login: login,
    register: register,
    users: users,
    students: students,
};
